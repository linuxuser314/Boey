# Boey
This was an attempt at a remote-controlled robot. The controller is in the repository `linuxuser314/Dash`. This uses myiolib.h and it is supposed to transmit and recieve data to dash. I never got it working and moved on.
